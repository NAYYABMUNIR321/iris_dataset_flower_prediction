# iris_dataset_flower_prediction
Predict the type of Iris flower based on sepal and petal measurements. Enter the values, click Predict, and the app uses an SVM model to determine the flower type. It then displays an image of the predicted Iris flower.
